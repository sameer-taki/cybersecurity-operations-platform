# ADR 0005: external AI egress disabled

Status: accepted. Data residency and customer approval for external LLM processing are unresolved. External AI egress is disabled by default, controlled by configuration, and fails closed when disabled. No provider or region is hard-coded.
